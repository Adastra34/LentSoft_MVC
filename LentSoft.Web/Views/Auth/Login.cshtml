@model LoginViewModel
@{
    ViewData["Title"] = "Iniciar Sesión";
}

<main style="min-height: calc(100vh - 140px); background: linear-gradient(135deg, var(--purple-50), var(--purple-100)); display: flex; align-items: center; justify-content: center; padding: 2.5rem 1rem;">
    <div style="width: 100%; max-width: 900px; background: white; border-radius: 1.25rem; box-shadow: var(--shadow-xl); overflow: hidden; display: grid; grid-template-columns: 1fr 1fr;">
        
        <!-- Left Column: Background Image -->
        <div style="position: relative; min-height: 480px; background-image: url('https://images.unsplash.com/photo-1572635196237-14b3f281503f?auto=format&fit=crop&q=80'); background-size: cover; background-position: center;">
            <div style="position: absolute; inset: 0; background: linear-gradient(to right, rgba(59, 7, 100, 0.4), rgba(124, 58, 237, 0.2));"></div>
            <div style="position: absolute; bottom: 2rem; left: 2rem; right: 2rem; color: white; z-index: 2;">
                <h3 style="color: white; font-size: 1.5rem; font-weight: 700; margin-bottom: 0.5rem; font-family: var(--font-primary);">LentSoft Optical</h3>
                <p style="color: var(--purple-100); font-size: 0.875rem; margin: 0;">Tu salud visual en las mejores manos.</p>
            </div>
        </div>

        <!-- Right Column: Form -->
        <div style="padding: 3rem 2.5rem; display: flex; flex-direction: column; justify-content: center; background: white;">
            <h1 style="text-align: center; margin-bottom: 2rem; color: var(--purple-700); font-size: 1.5rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.5px; font-family: var(--font-primary);">
                INICIO DE SESIÓN
            </h1>

            <form asp-controller="Auth" asp-action="Login" method="post" style="display: flex; flex-direction: column; gap: 1.25rem;">
                <input type="hidden" name="returnUrl" value="@ViewBag.ReturnUrl" />
                @if (TempData["SuccessMessage"] != null)
                {
                    <div style="background-color: #ECFDF5; border: 1px solid #A7F3D0; border-radius: 0.75rem; padding: 1rem 1.25rem; text-align: center;">
                        <p style="color: #065F46; font-size: 0.875rem; font-weight: 500; margin: 0;">
                            ✅ @TempData["SuccessMessage"]
                        </p>
                    </div>
                }
                <style>
                    .alert-error.validation-summary-valid {
                        display: none !important;
                    }
                    .alert-error.validation-summary-errors {
                        display: flex;
                        align-items: center;
                        gap: 0.75rem;
                    }
                    .alert-error.validation-summary-errors ul {
                        list-style: none;
                        margin: 0;
                        padding: 0;
                    }
                    .alert-error.validation-summary-errors li {
                        font-size: 1rem;
                        font-weight: 500;
                    }
                    .alert-error.validation-summary-errors::before {
                        content: "⚠️";
                        font-size: 1.25rem;
                        line-height: 1;
                        flex-shrink: 0;
                    }
                </style>
                <div asp-validation-summary="ModelOnly" class="alert alert-error" style="font-size: 1rem; font-weight: 500; padding: 1rem 1.25rem; margin-bottom: 1.25rem; background-color: #FEF2F2; border-left: 4px solid var(--error); color: #991B1B; border-radius: 0.5rem;"></div>

                <!-- Email Field (No visible label) -->
                <div class="form-group" style="margin-bottom: 0;">
                    <input asp-for="Email" placeholder="Correo electrónico" class="form-input" style="border-radius: 9999px; padding: 0.75rem 1.25rem; border: 1.5px solid var(--border);" />
                    <span asp-validation-for="Email" style="color: var(--error); font-size: 0.75rem; padding-left: 0.75rem; display: block; margin-top: 0.25rem;"></span>
                </div>

                <!-- Password Field with Toggle Eye Icon -->
                <div class="form-group" style="margin-bottom: 0;">
                    <div style="position: relative;">
                        <input asp-for="Password" placeholder="Contraseña" class="form-input" style="border-radius: 9999px; padding: 0.75rem 3rem 0.75rem 1.25rem; border: 1.5px solid var(--border);" id="passwordInput" />
                        <button type="button" id="togglePasswordBtn" style="position: absolute; right: 1rem; top: 50%; transform: translateY(-50%); background: none; border: none; cursor: pointer; color: var(--gray-500); font-size: 1.1rem;" title="Mostrar/Ocultar contraseña">
                            👁️
                        </button>
                    </div>
                    <span asp-validation-for="Password" style="color: var(--error); font-size: 0.75rem; padding-left: 0.75rem; display: block; margin-top: 0.25rem;"></span>
                </div>

                <!-- Forgot Password Link -->
                <div style="text-align: left;">
                    <a asp-controller="Auth" asp-action="ForgotPassword" style="color: var(--purple-700); font-size: 0.85rem; font-weight: 500;">
                        ¿Olvidaste tu contraseña?
                    </a>
                </div>

                <!-- Submit Button (Slate Dark / Full Width / Rounded) -->
                <button type="submit" class="btn" style="width: 100%; background-color: var(--gray-800); color: white; border-radius: 9999px; padding: 0.8rem; font-size: 0.95rem; font-weight: 600; margin-top: 0.5rem; transition: background-color 0.2s;" onmouseover="this.style.backgroundColor='#111827'" onmouseout="this.style.backgroundColor='var(--gray-800)'">
                    Iniciar sesión
                </button>

                <!-- Create Account Link -->
                <div style="text-align: center; margin-top: 0.75rem;">
                    <a asp-controller="Auth" asp-action="Register" style="color: var(--purple-700); font-size: 0.9rem; font-weight: 600; text-decoration: underline; text-underline-offset: 3px;">
                        Crear cuenta
                    </a>
                </div>
            </form>
        </div>

    </div>
</main>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const passwordInput = document.getElementById('passwordInput');
            const togglePasswordBtn = document.getElementById('togglePasswordBtn');

            if (togglePasswordBtn && passwordInput) {
                togglePasswordBtn.addEventListener('click', function () {
                    const isPassword = passwordInput.type === 'password';
                    passwordInput.type = isPassword ? 'text' : 'password';
                    togglePasswordBtn.textContent = isPassword ? '🙈' : '👁️';
                });
            }
        });
    </script>
}
